# RPC URL for Ethereum Mainnet (or your target chain)
# Example: https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY
RPC_URL=https://eth.public.zkevm-test.net:8545

# Block number where the vulnerable contract exists
# Use a recent block number to ensure the contract is deployed
# For mainnet testing, use a recent block (e.g., 19000000)
BLOCK_NUMBER=
